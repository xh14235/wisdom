let defaultToken = sessionStorage.token || ''

export default {
  tab_left: '001',
  tab_right: '001',
  weatherControlShow: false,
  token: defaultToken,
  selectListShow: false,
  leftTimer: true,
  rightTimer: false
}
